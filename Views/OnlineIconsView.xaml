<UserControl x:Class="RadiMenu.Views.OnlineIconsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox x:Name="OnlineSearchBox" 
                     Height="32"
                     VerticalContentAlignment="Center"
                     Padding="5,0"
                     Background="#2D2D30"
                     Foreground="White"
                     BorderThickness="0"
                     KeyDown="OnlineSearchBox_KeyDown"
                     Text="music"/>
            
            <Button Grid.Column="1" 
                    Content="Buscar" 
                    Width="80"
                    Margin="10,0,0,0"
                    Click="BtnSearch_Click"/>
        </Grid>

        <!-- Filters (Optional, keeping simple for now) -->
        <!-- <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Results for: " Foreground="Gray" VerticalAlignment="Center"/>
            <TextBlock x:Name="ResultCountText" Text="0" Foreground="White" VerticalAlignment="Center" Margin="5,0,0,0"/>
        </StackPanel> -->
        <TextBlock x:Name="StatusText" Grid.Row="1" Text="Escribe algo y presiona Enter..." Foreground="Gray" Margin="0,0,0,5"/>

        <!-- Results Grid -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="OnlineIconsPanel" />
        </ScrollViewer>

        <!-- Loading Indicator -->
        <Border x:Name="LoadingOverlay" Grid.RowSpan="4" Background="#AA000000" Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Buscando..." Foreground="White" FontSize="16"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
