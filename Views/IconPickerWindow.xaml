<Window x:Class="RadiMenu.Views.IconPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:RadiMenu.Views"
        Title="Seleccionar Icono" 
        Width="700" Height="600"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TabControl Grid.Row="0" Margin="0,0,0,15" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#CCCCCC"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Margin" Value="0,0,10,0"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Border" 
                                        Background="{TemplateBinding Background}" 
                                        BorderBrush="{TemplateBinding BorderBrush}" 
                                        BorderThickness="0,0,0,2" 
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="BorderBrush" Value="#0078D4"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- TAB 1: System Icons (Fluent) -->
            <TabItem Header="Sistema (Windows)">
                <Grid Margin="0,15,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBox x:Name="SearchBox" 
                             Grid.Row="0"
                             Margin="0,0,0,15"
                             Padding="10"
                             Background="#2D2D30"
                             Foreground="White"
                             BorderThickness="0"
                             TextChanged="SearchBox_TextChanged"/>
                    
                    <ScrollViewer Grid.Row="1" 
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <WrapPanel x:Name="IconsPanel"/>
                    </ScrollViewer>
                </Grid>
            </TabItem>
            
            <!-- TAB 2: Online Icons (Universal) -->
            <TabItem Header="Online (Universal)">
                <views:OnlineIconsView x:Name="OnlineView" Margin="0,15,0,0"/>
            </TabItem>
        </TabControl>
        
        <!-- Bottom bar -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock x:Name="SelectedLabel" 
                       Text="Ninguno seleccionado"
                       Foreground="#888888"
                       VerticalAlignment="Center"/>
            
            <Button Grid.Column="1" 
                    Content="Cancelar" 
                    Width="80"
                    Height="32"
                    Margin="0,0,10,0"
                    Click="Cancel_Click"/>
            
            <Button Grid.Column="2" 
                    Content="Seleccionar" 
                    Width="100"
                    Height="32"
                    Background="#0078D4"
                    Click="Select_Click"/>
        </Grid>
    </Grid>
</Window>
